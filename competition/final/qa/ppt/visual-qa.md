# PPT 视觉 QA 记录

## 首轮渲染（render-v1）

- 逐页导出 12 张 PNG，并生成 `contact-sheet-v1.png`。
- 发现封面中英文混排的 `Agent` 与第 5/6 页的 `BOQ` 出现字体替代符号或异常字距。
- 修正：混合中英文的标题使用系统中文字体回退；中文主标题继续声明 Source Han Sans SC Bold。

## 第二轮渲染（render-v2）

- 重新导出全套 12 页，并生成 `contact-sheet-v2.png`。
- 字体替代问题已消失。
- 放大检查第 6 页，发现第五步“单价合成”被右侧工作底稿面板遮挡。
- 修正：压缩五步阶梯的水平步距，为第五步与工作底稿之间留出净空。

## 最终复验（render-v3）

- 重新导出全套 12 页，生成 `contact-sheet-final.png`。
- 单独以 150 dpi 复验第 6 页：五个步骤标题与说明均完整可见，未与工作底稿重叠。
- 全套总览未发现标题或正文裁切、低对比、页面边界越界、残留占位符。
- `layout-check.json`：716 个已知对象，边界问题 0。
- `content-check.json`：12 页，13.333 × 7.5 英寸，禁用口径命中 0，占位符命中 0。

## 独立审查后修正（render-v4 / render-v5）

- 将第 2—9、11—12 页的 kicker 与页码统一收进顶部 0.5 英寸安全区，将公共页脚、封面参赛信息和第 10 页页脚收进底部 0.5 英寸安全区；第 12 页底部原则卡同步上移，避免与页脚争抢空间。
- 补强自动检查：所有非全出血文本与图片同时检查左、上、右、下四边安全区。最终记录 721 个对象，安全边距、页面越界与无效尺寸问题均为 0。
- 第 8 页对项目路径与知识库路径分别增加不透明白色遮罩，并替换为“路径已脱敏”标签；放大检查未再显示 `C:\Users\xiang\Desktop\N3-section1` 或 AppData 知识库路径。
- 第 7 页改为“系统内可点击回源”，同时明确“本页为静态示意”，避免将 PPT 卡片误解为现场可点击链接。
- `render-v5` 重新导出全部 12 页并覆盖 `contact-sheet-final.png`。重点复验第 1、7、8、10、11、12 页后，未发现新裁切、遮挡、低对比或安全边距问题。

## 最终递交包收口（render-v6）

- 不再依赖 PPT 遮罩：从原始知识库截图生成 `assets/kb-workbench-redacted.png`，将两条路径的遮挡直接写入像素；第 8 页改为只嵌入该脱敏图，不再叠加路径遮罩对象。
- 解压最终 PPTX 的全部 10 个媒体文件。`image-8-1.png` 与脱敏资产 SHA-256 一致；媒体中与原始未脱敏截图哈希一致的文件为 0。
- 生成 `media-contact-sheet-final.png` 并逐项查看全部媒体；知识库截图中只保留“项目路径已脱敏 / 知识库路径已脱敏”，未见 `C:\Users\xiang` 或 `AppData`。
- 扫描 PPTX 全部包条目的 ASCII 内容，敏感路径命中 0；幻灯片文本中旧的作品名空格形式命中 0，正式作品名命中 8。
- `render-v6` 重新导出 12 页 PDF/PNG，更新 `contact-sheet-final.png`。第 8 页像素脱敏、主体画面、页脚安全区均正常。
- 最终 `layout-check.json` 记录 717 个对象，四边安全区、页面越界与无效尺寸问题均为 0。
