# Agent π 3.2.3 — 文件夹只给地址，右键注入对话

**3.2.2** 卸掉了 J-Space。**3.2.3** 修两处对话附件：

- 输入框旁边的文件夹按钮不再把整夹文件拷进工作区。选完只挂一个文件夹芯片（图标 + 名称），把绝对路径交给模型去列目录、按需读取。
- 右侧「资源文件」里右键重新出现 **注入对话**。菜单提到最上层，文件夹预览里也能右键。

右侧工具栏的「上传文件夹」仍会把资料拷进工作区，那是导入，不是对话地址。

覆盖安装前请完全退出（不要只关到托盘）。
