# Release policy

Agent Pi DSH 的公开版本与资源默认不可变；经用户明确授权的例外在本文中单独记录。

1. 按用户要求，功能和内核升级默认沿用当前分支；除非用户明确要求，不新增分支或 worktree。保留已有功能及用户未提交的工作，不从较旧快照重新组装升级。
2. 标签从已验证的 `main` 提交创建；不得移动或复用已经公开的标签，明确授权的例外除外。
3. `DSH_PIN` 每次变化都必须提升 Agent Pi DSH 的应用版本。
4. Release 先保持 draft；Windows、macOS、Linux 和运行时资源全部成功后才发布为 Latest。
5. 上传命令不得使用 `--clobber`。资源有误时提升版本重新发布，不覆盖同版本。

`kernel-version-policy` CI 比较 PR base 与 head；Windows 和跨平台打包还会核验 `release/kernel-version-history.json`。

## 2026-09-06：3.6.2 安装器故障修复例外

用户明确要求本地重打并替换 GitHub 3.6.2 的 Windows 安装包，同时撤下其他平台资源后重新生成。此次授权覆盖同版本安装包、运行时载荷及其校验文件和构建回执的替换，不改变应用版本或 `DSH_PIN`，也不授权今后的版本默认覆盖资源。按用户选择，本次不执行真实产品安装、升级或卸载；采用生产 NSIS 隔离升级 fixture 和解包应用独立启动验证。

- 此次允许将 `v3.6.2` 移到包含修复并完成验证的 `main` 提交。原 annotated tag object 为 `02ea52b7f25c38defa1c9593582ae3ab90c685ee`，原提交为 `1aef6820ebc450125788158a4b2d1706115cdd10`；原来源保留于此，新构建提交以随附构建回执及 GitHub Actions 记录为准。
- 允许此次替换已公开的同名资源；替换前保留原资源清单、SHA256 和构建回执，完成后重新核对下载资产、校验文件和回执。平台资源撤下及重建期间，应明确标注尚未完成的部分。
- CAD 可复用原已验证产物，前提是实际 CAD 源码、依赖 pin、工具链和构建输入未变化，并再次通过原对应源码与 runtime 的完整性验证。保留原 CAD 来源提交、源码归档及回执，不改写为本次安装器的提交；本次安装器修复源码由新的应用提交提供。
- 此次同时修正误排除官方 Office 插件的打包规则：包含固定版本的完整 `dsh-univer-office`，验证来源回执、运行时依赖及原始许可材料，保留上游授权校验。该例外不放宽官方 DSH 源码完整性或安装后已声明文件校验。若 CAD 构建输入变化，应重新生成与其对应的产物和源码。
