# 3.6.3 验证记录

## 来源

- 基线：3.6.2 发布提交 `750df01094cd52fce8b9a8a67e303a8c81ac44d6`，沿用既有发布分支，无新增分支或工作树。
- DSH：`2faa751be99c8fbee0524e478f4c53d93b408131` / `0.1.5-alpha.1`；锁定依赖安装、官方 `clean`、`build:lib`、`build:web` 完成，官方源码守卫通过。
- Univer：保留完整官方 0.2.13 包及原始许可/授权校验。
- CAD：保留原始 3.6.2 构建和源码归档。仅允许应用版本字段及既有 Office 许可段落差异；CAD 源码、依赖、工具链、运行时资源仍须通过原始校验。

## 已执行的兼容性检查

- 配置、灰度模型迁移、Univer 适配及运行时版本校验：36 项通过；切换官方文件锁接口后的运行时/CAD 检查也通过。
- 产品 host/client 插件回归：首轮 447 项中 445 项通过，另两项缺少尚在安装的 Codex 依赖和未复制的 CAD 资源。依赖/资源就绪后对应 10 项专项测试全部通过；未修改产品业务逻辑来绕过失败。
- 官方 `v2-system-migration`、`v2-ptc-migration`、`multi-edge-publication`、`win32`：4 个文件、30 项测试通过。
- CAD 跨版本复用约束：24 项通过；运行时回执及便携包阶段：28 项通过；版本/打包约束：19 项通过。
- 真实独立 DSH 进程：Standard/PTC/Cordis 普通会话、显式项目绑定及父子孙会话边界通过；普通会话不注入投标执行流程。模型目录包含指定灰度模型。
- 浏览器：XLSX、DOCX、PPTX 的 Gateway 导入和 Viewer 实际渲染通过；实际 `.univer` 文件可重新打开。页面错误为零。
- CAD 浏览器：本地 Worker/WASM 解析 DXF，等待绘制完成后确认 `MTEXT 中文`、`AGENT PI TEXT` 和几何线条实际显示。
- 知识库浏览器：新普通会话默认空；主动选择 A 后，另一个新会话仍为空，原会话选择 A 得到保留；未自动创建或启动投标项目。

Browser 插件未提供，界面检查使用现有 Playwright 和本机 Chrome。测试使用独立工作区、数据目录和私有端口，不启动真实安装/卸载程序，不访问用户模型认证，不进行付费模型推理。灰度模型只验证注册与选择配置，未验证账号服务端权限或推理结果。

## 发行物验收

正式产物由现有 Windows 打包流程、跨平台工作流和 `publish-v3.6.3-release.mjs` 校验。安装包身份以同一 Release 的 `.sha256`、Windows `.build.json` 及 Actions 记录为准；源码和浏览器通过不替代安装包检查，全部平台资源校验完成后才公开发布。
